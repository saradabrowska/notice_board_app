{% extends 'layout.html.twig' %}

{% block title %}{{ 'title.user_add'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'title.user_add'|trans }}</h1>
    {{ form_start(form, { method: 'post', action: url('user_add') }) }}
    {{ form_widget(form) }}
    <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-success" />
    {{ form_end(form) }}
{% endblock %}