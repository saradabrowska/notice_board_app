<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>{{ 'title.title'|trans }}</title>

    <!-- Bootstrap -->
    <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">
    <!-- <link href="{{ asset('css/main.css') }}" rel="stylesheet">
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>
<body>
<!--<nav class="navbar navbar-default navbar-fixed-top" id="nav">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
<div class="navbar-header">

    <a class="navbar-brand" href="{{ url('homepage') }}" title="{{ 'title.homepage'|trans }}">
        {{ 'title.homepage'|trans }}
    </a>
</div>

<!-- Collect the nav links, forms, and other content for toggling -->
<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav">
        <li>
            <a href="{{ url('offer_index') }}" title="{{ 'title.offers_list'|trans }}">
                {{ 'title.offers_list'|trans }}
            </a>
        </li>

    </ul>

    <ul class="nav navbar-nav navbar-right">
        <li> {% if is_granted('IS_AUTHENTICATED_FULLY') %}

        <li>      <a class="btn btn-default" href="{{ url('my_offers_paginated') }}" title="{{ 'myoffers'|trans }}">
                {{ 'myoffers'|trans }}
            </a></li>
        <li>      <a class="btn btn-default" href="" title="{{ 'user_edit'|trans }}">
                {{ 'user_edit'|trans }}
            </a></li>

        <li>  <a class="btn btn-default" href="" title="{{ 'favourite_offers'|trans }}">
                {{ 'favourite_offers'|trans }}
            </a></li>

        <li><a class="btn btn-default" href="{{ url('auth_logout') }}" title="{{ 'auth.logout.label'|trans }}">
                {{ 'auth.logout.label'|trans }}
            </a></li>
        {% else %}
        <a class="btn btn-default" href="{{ url('auth_login') }}" title="{{ 'auth.login.label'|trans }}">
            {{ 'auth.login.label'|trans }}
        </a>
        <a class="btn btn-default" href="{{ url('user_add') }}" title="{{ 'user.add'|trans }}">
            {{ 'user.add'|trans }}
        </a>
        {% endif %}</li>
    </ul>
</div><!-- /.navbar-collapse -->
</div><!-- /.container-fluid -->
<!--</nav>-->
<div class="container">
    <div class="row">
        <div class=".col-md-6 .col-md-offset-3" id="about">
            {% block body %}
            {% endblock %}
        </div>
    </div>
</div>
<footer>

</footer>

{% include 'default/_flash_messages.html.twig' %}
<!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
<!-- Include all compiled plugins (below), or include individual files as needed -->
<script src="js/bootstrap.min.js"></script>
</body>
</html>